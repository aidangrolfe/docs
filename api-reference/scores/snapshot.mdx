---
title: "Get snapshots for each action in the latest score events for a fixture"
api: "GET /scores/snapshot/{fixtureId}"
description: "Get snapshots for each action in the latest score events for a fixture"
---

Get snapshots for each action in the latest score events for a fixture.

## Path Parameters

<ParamField path="fixtureId" type="int64" required>
  The ID of the fixture
</ParamField>

## Query Parameters

<ParamField query="asOf" type="int64">
  Optional Unix timestamp (ms) for the latest time the historical snapshots are returned for. If omitted, returns the live snapshot.
</ParamField>

## Response

<ResponseField name="data" type="array<Scores>">
  Array of Scores objects containing score event data
</ResponseField>

### Scores Object

The Scores object contains detailed information about score events and statistics for a fixture. The structure varies by sport type (Basketball, Football, etc.) and includes:

<ResponseField name="FixtureId" type="int64">
  Fixture identifier
</ResponseField>

<ResponseField name="Seq" type="int32">
  Sequence number of the score event
</ResponseField>

<ResponseField name="Ts" type="int64">
  Timestamp of the score event
</ResponseField>

<ResponseField name="Stats" type="object">
  Map of statistical keys to their values
</ResponseField>

<ResponseField name="Sport" type="string">
  Sport type (Basketball, UsFootball, etc.)
</ResponseField>
